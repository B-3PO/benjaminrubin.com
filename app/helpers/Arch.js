export default class Arch {
  constructor(startX, startY, endX, endY, startAngle, endAngle, radius, enterDirection, direction, time) {
    this.startX = startX;
    this.startY = startY;
    this.endX = endX;
    this.endY = endY;
    this.startAngle = startAngle;
    this.endAngle = endAngle;
    this.radius = radius;
    this.enterDirection = enterDirection;
    this.direction = direction;
    this.time = time;
  }
}
