export default class Path {
  constructor(startX, startY, endX, endY, direction, time) {
    this.startX = startX;
    this.startY = startY;
    this.endX = endX;
    this.endY = endY;
    this.direction = direction;
    this.time = time;
  }
}
